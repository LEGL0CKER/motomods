# MotoMods

Mockup website for MotoMods — custom 3D-printed electric bikes.

Static single-page site (landing + mock shop). No real checkout.

## Run locally

```
npm install
npm start
```

Deployed on Railway (Node + serve).
